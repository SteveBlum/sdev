version: 2
updates:
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      docker-minor-dependencies:
        update-types:
        - "minor"
        - "patch"
      docker-major-dependencies:
        update-types:
        - "major"
